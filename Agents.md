# @sandlada/jss

## Project Overview

**@sandlada/jss** 簡稱 jss 是一個簡單的 CSS-in-JS 函式庫，旨在提供一種方便的方式來在 JavaScript 中定義和管理 CSS 樣式。它允許開發者使用 JavaScript 物件來描述樣式。

jss 主要聚焦在 sandlada 内部使用上而非公開使用。

## 代碼規範

- 使用函數式編程風格，避免副作用。
- 使用 TypeScript 進行靜態類型檢查，確保代碼的可靠性。
- 遵循一致的命名規則和代碼風格，以提高可讀性和維護性。
